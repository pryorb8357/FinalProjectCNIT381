---
# ------------------------------------------------------------
# Banner for all routers and switches
# ------------------------------------------------------------
- name: Add login banner to all devices
  hosts: routers, switch
  gather_facts: no
  tasks:
    - name: Configure login banner
      cisco.ios.ios_config:
        lines:
          - banner login ^C
          - Unauthorized access is prohibited.
          - All activities are monitored and recorded.
          - Disconnect immediately if you are not an authorized user.
          - ^C

# ------------------------------------------------------------
# R1 interface configuration
# ------------------------------------------------------------
- name: Configure interfaces on R1
  hosts: R1
  gather_facts: no
  tasks:
    - name: Configure R1 interfaces
      cisco.ios.ios_config:
        lines:
          - interface GigabitEthernet0/0/0
          - description R2-Gi0/0/0
          - ip address 172.168.1.1 255.255.255.252
          - no shutdown
          - interface GigabitEthernet0/0/1
          - description S1-F0/1
          - ip address 10.1.1.1 255.255.255.0
          - no shutdown

# ------------------------------------------------------------
# R2 interface configuration
# ------------------------------------------------------------
- name: Configure interfaces on R2
  hosts: R2
  gather_facts: no
  tasks:
    - name: Configure R2 interfaces
      cisco.ios.ios_config:
        lines:
          - interface GigabitEthernet0/0/0
          - description R1-Gi0/0/0
          - ip address 172.168.1.2 255.255.255.252
          - no shutdown
          - interface GigabitEthernet0/0/1
          - description S1-F0/2
          - ip address 10.1.1.2 255.255.255.0
          - no shutdown
